/* Two items needed to create AWS Lambda function to interact with Firebase 
 * (Must be in same directory):
 * 1) index.js (Firebase sdk code)
 * 2) Node modules folder (Instructions to create/config provided below)
 *
 * Compress both and send zip to AWS Lambda function 
 * (Ensure Timeout in Settings at least 5 sec)
 */

// To install node_modules folder
npm install firebase --prefix=./

// To get ensure proper version to match AWS: my_lambda\node_modules\grpc\src\node\extension_binary\node-v57-linux-x64-glibc\grpc_node.node
npm rebuild grpc --target=8.1.0 --target_arch=x64 --target_platform=linux --target_libc=glibc
